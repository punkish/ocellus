# Installation

The website is hosted on Github. The following commands use `gulp` to build the website and copy it under `docs/`. The repo is pushed to Github and hosted via Github Pages.

```sh
$ npx gulp
$ git add .
$ git commit -m <message>
$ git push
```